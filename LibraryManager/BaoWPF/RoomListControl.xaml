<UserControl x:Class="BaoWPF.RoomListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button Content="Add Room" Margin="5" Click="btnAdd_Click"/>
            <Button Content="Edit Room" Margin="5" Click="btnEdit_Click"/>
            <Button Content="Delete Room" Margin="5" Click="btnDelete_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgRooms" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding RoomId}" Width="50"/>
                <DataGridTextColumn Header="Number" Binding="{Binding RoomNumber}" Width="100"/>
                <DataGridTextColumn Header="Description" Binding="{Binding RoomDescription}" Width="150"/>
                <DataGridTextColumn Header="Capacity" Binding="{Binding RoomMaxCapacity}" Width="80"/>
                <DataGridTextColumn Header="Price/Day" Binding="{Binding RoomPricePerDate}" Width="100"/>
                <DataGridTextColumn Header="Status" Binding="{Binding RoomStatus}" Width="80"/>
                <DataGridTextColumn Header="Room Type" Binding="{Binding RoomType.RoomTypeName}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>