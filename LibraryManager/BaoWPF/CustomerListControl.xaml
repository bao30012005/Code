<UserControl x:Class="BaoWPF.CustomerListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button Content="Edit" Margin="5" Click="btnEdit_Click"/>
            <Button Content="Delete" Margin="5" Click="btnDelete_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgCustomers" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CustomerId}" Width="60"/>
                <DataGridTextColumn Header="Full Name" Binding="{Binding CustomerFullName}" Width="150"/>
                <DataGridTextColumn Header="Email" Binding="{Binding EmailAddress}" Width="200"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Telephone}" Width="120"/>
                <DataGridTextColumn Header="Birthday" Binding="{Binding CustomerBirthday}" Width="100"/>
                <DataGridTextColumn Header="Status" Binding="{Binding CustomerStatus}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>